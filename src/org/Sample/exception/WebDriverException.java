package org.Sample.exception;

public class WebDriverException {
	
	/* WebDriver Exception :-
	 * 
	 * 		WebDriver Exception occurs when the network is not connect properly or when the computer process is slow.
	 */
	
	
	
	/* WebDriver Exception with timeout Exception:-
	 * 
	 *		WebDriver Exception with timeout Exception is occurs when both network and computer processes are slow. 
	 */
	
	
	
	/* SessionNotCreatedException :-
	 * 
	 * 		SessionNotCreatedException is through when the browser version and driver version is different.
	 */

}
